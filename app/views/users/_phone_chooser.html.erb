 <div class="control-group">
  <% if user.verifed_phone? %>
    <% if user.verified_phones.length < 2 %>
      We will call you at: <%= user.verified_phone.formatted %> <%= link_to 'Add Another Phone', @logged_in_user, :class => '' %>
    <% else %>
    <% end %>
  <% else %>
    <p><strong>No verified phones found.</strong> <%= link_to 'Add Phone', @logged_in_user, :class => 'btn btn-inverse' %>
  <% end %>
</div>